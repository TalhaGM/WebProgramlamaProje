@{
    ViewData["Title"] = "Giriş Yap";
    Layout = $"~/Views/Shared/{(string)ViewBag.Layout}.cshtml";
}

<h2>@ViewData["Title"]</h2>

@if (ViewBag.Error != null)
{
    <div class="alert alert-danger">@ViewBag.Error</div>
}


